name: nodeserver

services:
  server:
    build:
      dockerfile: Dockerfile_app
      context: .
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_started

  postgres:
    build:
      dockerfile: Dockerfile_db
      context: .
    ports:
      - "5434:5432"
